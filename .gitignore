.env
__pycache__/
*.pyc
node_modules/
content/raw/
*.db
*.sqlite3
.venv/
*.egg-info/

# raw scraped candidates (un-anonymized, kept locally for review only)
content/assets/journals/candidates/
content/papers/

# Third-party curriculum PDFs (course material — not ours to redistribute)
*.pdf
# ...but the player handbook is ours, generated by scripts/handbook/build.py, and
# is published by the Pages workflow. The blanket rule above swallowed it silently
# — `git add` skips ignored files without complaining — so the site build failed
# on a file everyone believed was committed (#62). Exceptions stay explicit.
!docs/handbook-player/player-handbook.pdf

# Raw generation staging (finished assets live in content/assets/art/)
content/assets/art/incoming/
